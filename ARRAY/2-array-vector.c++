#include <iostream>
#include <vector>
using namespace std;
int main()
{
    vector<int> arr;
    arr.push_back(8);
    arr.push_back(81);
    arr.push_back(2);
    arr.push_back(65);
    arr.push_back(76);
}